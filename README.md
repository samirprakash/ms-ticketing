# Ticketing Micro Service

- Install `skaffold`
- Install `kubectl`
- Install `docker desktop`

- Run `yarn`
- Run `skaffold dev`

- Add entry to /etc/hosts `127.0.0.1 ticketing.dev`

- Browse to `http://ticket.dev`
